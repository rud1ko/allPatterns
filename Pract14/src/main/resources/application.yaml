spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/prac15db
    username: postgres
    password: papamama1R
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  main:
    allow-circular-references: true